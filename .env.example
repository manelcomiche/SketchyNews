# Optional: Server-side default OpenAI API key.
# If not set, users must provide their own key via the UI.
OPENAI_API_KEY=

# Optional: Custom OpenAI-compatible base URL (for proxies or alternative providers).
OPENAI_BASE_URL=
